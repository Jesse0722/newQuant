---
description: 当用户选择 subagent 驱动方式执行实施计划时触发，用于逐任务分派 subagent 并进行两阶段 review
globs:
alwaysApply: false
---

# Subagent-Driven Development — Subagent 驱动开发

> 基于 [Superpowers subagent-driven-development](https://github.com/obra/superpowers) skill，适配本项目。

## 概述

通过为每个任务分派独立 subagent 来执行计划，每个任务完成后进行两阶段 review：先规格合规审查，再代码质量审查。

**核心原则：** 每任务独立 subagent + 两阶段 review = 高质量 + 快速迭代

## 何时使用

- 有已批准的实施计划（`docs/plans/` 中的计划文件）
- 任务基本独立
- 在当前会话中执行

## 执行流程

### 准备阶段

1. 读取计划文件，**一次性提取所有任务的完整文本和上下文**
2. 用 TodoWrite 创建所有任务的跟踪列表

### 每个任务的循环

```
对每个任务：
  1. 分派实现 subagent（Task 工具）
     - 提供完整任务文本 + 项目上下文
     - 不要让 subagent 自己读取计划文件
  2. 如果 subagent 有问题 → 回答后重新分派
  3. subagent 实现、测试、提交、自审
  4. 分派规格审查 subagent
     - 确认代码是否匹配规格
     - 不合规 → 实现 subagent 修复 → 重新审查
  5. 分派代码质量审查 subagent
     - 检查代码质量
     - 有问题 → 实现 subagent 修复 → 重新审查
  6. 两项审查都通过 → 标记任务完成
```

### 完成阶段

- 所有任务完成后，分派最终代码审查 subagent 对整体实现进行 review
- 报告完成状态

## Subagent 分派模板

### 实现 Subagent

```
你是一个实现工程师。请完成以下任务：

## 项目背景
[项目简述和当前任务在整体中的位置]

## 任务详情
[从计划中提取的完整任务文本]

## 要求
- 遵循 TDD：先写测试，确认失败，再写实现，确认通过
- 完成后提交 git
- 自审代码，修复发现的问题
- 使用中文写提交信息
```

### 规格审查 Subagent

```
你是规格合规审查员。请审查以下任务的实现：

## 任务规格
[完整任务文本]

## 变更范围
[git diff 或相关文件]

## 审查要点
- 所有规格要求是否都已实现？
- 是否有超出规格的多余实现？
- 测试是否覆盖了规格中的所有场景？

输出：✅ 合规 / ❌ 不合规（列出具体问题）
```

### 代码质量审查 Subagent

```
你是代码质量审查员。请审查以下变更：

## 变更范围
[git diff 或相关文件]

## 审查要点
- 代码是否清晰、可维护？
- 是否遵循 DRY/YAGNI 原则？
- 错误处理是否充分？
- 是否有安全问题？
- 测试质量如何？

输出：✅ 通过 / ❌ 有问题（按严重程度列出）
```

## 红线规则

- **绝不**跳过审查（规格审查和代码质量审查都不能跳）
- **绝不**在审查有未修复问题时继续下一个任务
- **绝不**并行分派多个实现 subagent（会冲突）
- **绝不**让 subagent 自己读计划文件（由控制器提供完整文本）
- **必须**先完成规格审查，再做代码质量审查（顺序不能反）
- 如果 subagent 提问，**完整回答**后再让其继续
- 如果审查发现问题，修复后**必须重新审查**
