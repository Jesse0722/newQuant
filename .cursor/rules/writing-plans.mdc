---
description: 当有已批准的设计文档需要生成实施计划时触发，或用户明确要求制定实施计划时触发
globs:
alwaysApply: false
---

# Writing Plans — 生成实施计划

> 基于 [Superpowers writing-plans](https://github.com/obra/superpowers) skill，适配本项目。

## 概述

编写全面的实施计划，假设执行者对代码库零上下文。文档化他们需要知道的一切：每个任务要修改哪些文件、具体代码、如何测试。以小粒度任务呈现。DRY、YAGNI、TDD、频繁提交。

**启动时宣布：**「我正在使用 writing-plans 规则来创建实施计划。」

**保存位置：** `docs/plans/YYYY-MM-DD-<feature>-plan.md`

## 任务粒度

**每步是一个动作（2-5 分钟）：**

- 「编写失败的测试」— 一步
- 「运行测试确认失败」— 一步
- 「编写最小代码让测试通过」— 一步
- 「运行测试确认通过」— 一步
- 「提交」— 一步

## 计划文档头部

**每个计划必须以此头部开始：**

```markdown
# [功能名称] 实施计划

> **执行方式：** 使用 subagent-driven-dev 规则逐任务实施。

**目标：** [一句话描述构建什么]

**架构：** [2-3 句话描述方案]

**技术栈：** [关键技术/库]

---
```

## 任务结构模板

````markdown
### 任务 N：[组件名称]

**文件：**
- 创建：`exact/path/to/file.py`
- 修改：`exact/path/to/existing.py:123-145`
- 测试：`tests/exact/path/to/test.py`

**步骤 1：编写失败的测试**

```python
def test_specific_behavior():
    result = function(input)
    assert result == expected
```

**步骤 2：运行测试确认失败**

运行：`pytest tests/path/test.py::test_name -v`
预期：FAIL，报错 "function not defined"

**步骤 3：编写最小实现**

```python
def function(input):
    return expected
```

**步骤 4：运行测试确认通过**

运行：`pytest tests/path/test.py::test_name -v`
预期：PASS

**步骤 5：提交**

```bash
git add tests/path/test.py src/path/file.py
git commit -m "feat: add specific feature"
```
````

## 核心要求

- 始终使用**精确文件路径**
- 计划中包含**完整代码**（不要写「添加验证逻辑」之类的模糊描述）
- 提供**精确命令**和预期输出
- DRY、YAGNI、TDD、频繁提交
- 始终使用**中文**描述任务

## 执行交接

保存计划后，提供执行选择：

**「计划已保存到 `docs/plans/xxx.md`。两种执行方式：」**

1. **Subagent 驱动（当前会话）** — 每个任务分派独立 subagent，任务间自动 review，快速迭代
2. **手动执行** — 按计划逐步实施，每批任务后人工确认

**「选择哪种方式？」**
