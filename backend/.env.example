# Tushare Pro API Token (必填，在 https://tushare.pro 获取)
TUSHARE_TOKEN=your_tushare_token_here
# SQLite 数据库路径 (相对于 backend/ 目录)
DATABASE_URL=sqlite:///./data/quant.db
# 数据目录 (相对于 backend/ 目录)
DATA_DIR=./data
